# Copy to application-local.yml and set your MySQL Workbench password.
# Spring Boot loads application-local.yml automatically when present.
spring:
  datasource:
    password: YOUR_MYSQL_ROOT_PASSWORD
